<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>
    <style name="StarterTheme" parent="android:style/Theme.NoTitleBar">
        <item name="android:background">@drawable/start_background</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="NewCurriculumTheme" parent="AppTheme">
        <item name="android:background">@null</item>
    </style>

    <style name="StartTheme" parent="android:style/Theme.NoTitleBar.Fullscreen">
        
    </style>
    <style name="AnimationPreview">
        <item name="android:windowEnterAnimation">@anim/mession_fade_in</item>
        <item name="android:windowExitAnimation">@anim/mession_fade_out</item>
    </style>

</resources>